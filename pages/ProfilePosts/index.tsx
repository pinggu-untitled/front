import React from 'react';
import styled from '@emotion/styled';

export const Base = styled.div`
  width: 100%;
  position: relative;
`;

const ProfilePosts = () => {
  return <Base>해당 유저의 게시물 목록</Base>;
};

export default ProfilePosts;
